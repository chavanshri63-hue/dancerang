{
  "name": "dancerang-firestore-rules",
  "version": "1.0.0",
  "description": "Firestore security rules and admin management for DanceRang app",
  "main": "scripts/setAdminClaim.js",
  "scripts": {
    "test:rules": "node scripts/test-firestore-rules.js",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes",
    "deploy:all": "firebase deploy",
    "admin:set": "node scripts/setAdminClaim.js set",
    "admin:remove": "node scripts/setAdminClaim.js remove",
    "admin:list": "node scripts/setAdminClaim.js list",
    "admin:get": "node scripts/setAdminClaim.js get"
  },
  "keywords": [
    "firebase",
    "firestore",
    "security-rules",
    "dancerang",
    "dance-app"
  ],
  "author": "DanceRang Team",
  "license": "MIT",
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase-tools": "^13.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
